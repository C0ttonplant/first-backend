# Fist Backend

this is the first backend api that i am making for myself. it will be super awesome and cool and everything.

## setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## running

```bash
# while in venv
uvicorn main:app --reload
```
